Based on everything you can see in this repo, produce `ONBOARDING.md` — a no-fluff developer quickstart:

1. **Prerequisites** — exact versions of Node, Python, Docker, etc. needed
2. **Setup steps** — clone to running locally, numbered, copy-pasteable commands
3. **Key commands** — dev server, build, test, lint, deploy (as a table)
4. **Repo conventions** — file naming, import conventions, commit message format, PR process (inferred from configs and any contributing docs)
5. **Where things live** — "if you need to change X, look in Y" for the 10 most common tasks
6. **Common gotchas** — anything that looks like it would trip up a new dev (weird build steps, order-dependent setup, known workarounds visible in comments/configs)

Write this for a senior engineer who doesn't need hand-holding but does need to know where everything is.